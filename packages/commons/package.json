{
  "name": "@project-leek/commons",
  "version": "0.0.1",
  "main": "dist/",
  "types": "dist/",
  "scripts": {
    "start": "tsc --watch",
    "build": "yarn clean && tsc",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "typecheck": "tsc --noEmit",
    "clean": "shx rm -rf dist/",
    "reset": "yarn clean"
  },
  "dependencies": {
    "@types/uuid": "8.3.0",
    "uuid": "^8.3.1"
  }
}
